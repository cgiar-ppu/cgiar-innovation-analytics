# Synapsis Analytics Agent — macOS native dependencies
# Install: pip install -r requirements-macos.txt

# Core
claude-agent-sdk>=0.1.0
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
websockets>=14.0
python-multipart>=0.0.18
aiofiles>=24.1.0
aiosqlite>=0.20.0

# Data science
pandas>=2.2.0
numpy>=1.26.0
scipy>=1.12.0
openpyxl>=3.1.0
xlrd>=2.0.0
xlsxwriter>=3.2.0

# Semantic search (prms_search hybrid index — Qwen3-Embedding-0.6B on MPS).
# Lazy-loaded at first search; the MCP tool degrades gracefully if absent.
# FTS5 BM25 uses the stdlib sqlite3; rank_bm25 is listed as an explicit
# fallback per the hybrid-search design.
sentence-transformers>=5.5.0
torch>=2.2.0
scikit-learn>=1.4.0
rank_bm25>=0.2.2

# Visualization
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.18.0

# Document generation
python-docx>=1.1.0
python-pptx>=0.6.23
reportlab>=4.1.0
pypdf2>=3.0.0
pdfplumber>=0.11.0

# Web / HTTP
requests>=2.31.0
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.1.0

# macOS native input (CGEvent for key delivery and scroll)
pyobjc-framework-Quartz>=10.0

# Utilities
tabulate>=0.9.0
rich>=13.7.0
tqdm>=4.66.0
pyyaml>=6.0.0
toml>=0.10.0
jupyter>=1.0.0
nbformat>=5.10.0
markdown>=3.5.0
Pillow>=10.2.0

# Testing
pytest>=9.0.0
pytest-asyncio>=1.0.0
