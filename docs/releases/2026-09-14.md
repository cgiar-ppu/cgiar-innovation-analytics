# September 2026 promotion and model refresh

Jose authorized DEV → staging → production, model updates and live QA in chat 44f6ffcc999d. No push to main is used. The baseline application is f55374d; the final model release SHA is recorded in the deployment report.

## Environments

- DEV: https://innovation-analytics-dev.synapsis-analytics.com — AWS 972793825893.
- Staging/testing: https://innovation-analytics-staging.synapsis-analytics.com — AWS 053142643230, stack cgiar-ia-staging.
- Production: https://innovation-analytics.synapsis-analytics.com — AWS 207258148366, existing stack cgiar-ia-prod. The release switches the website CNAME from the legacy CloudFront frontend to the existing production ALB. api-innovation continues to reach that same backend.

SSO deliberately shares the verified CGIAR identity pool eu-central-1_QDmB1qeBE and AzureAD provider in DEV's account. It has separate application clients: DEV 7vu6dhatj39qsbe21t58cr38an; staging 2k6tqfcpmdmudriqcr0pu9o79k; production 32vur1e407dvm36orsftub7bir. Each client allows exactly its own HTTPS callback and logout origin. App JWT keys, SQLite data, invitation records and refresh sessions stay separate per environment. No Entra secret is copied or new Microsoft permission granted. The previously verified Jose subject therefore refers to the same CGIAR identity in all clients.

This topology was selected to complete the authorized unattended rollout using the existing tested upstream registration. Identity administration and credential rotation are shared dependencies. Microsoft's consent screen may retain the registration's DEV name. Separate pools/registrations remain a future isolation option and require an explicit migration plan.

## Models and outputs

The model catalog adds Sonnet 5, Opus 5 and Fable 5.1; Sonnet 5 is the default and Opus 5 the configured fallback. The two former options remain for compatibility. Builtin specialists use explicit claude-sonnet-5, retaining the existing all-Sonnet policy including legacy `_opus_powerful` routing names. Custom `sonnet`/`opus` aliases resolve to current Sonnet 5/Opus 5 when loaded; explicitly chosen model IDs are preserved. The Agent SDK is pinned to 0.2.152.

Every loaded specialist and the main orchestrator receives the shared zero-draft output instructions. Canonical wording remains `AI V0 DRAFT — REQUIRES HUMAN VALIDATION`. Built-in exports use the existing watermark utility. Chromium is included in the runtime to support actual PDF export, which the baseline silently served as HTML fallback. Output instructions also cover spreadsheets, slides and standalone charts created by agents.

## Promotion and rollback

DEV uses the existing feature/innovation-platform-foundation push lane. release-promote.yml copies the already-built immutable SHA image from DEV ECR to the target account, then runs release-deploy.py/release-host.py through OIDC and SSM. Existing production infrastructure is not updated/replaced. The initial staging stack uses bootstrap-only userdata so no unauthenticated app starts before configuration.

Before replacement, preserve unmounted legacy workspace files, stop the old container, snapshot ownership/counts, and back up every SQLite database. Retain the renamed old container plus backups under `/opt/cgiar-ia/release-backups/<UTC timestamp>`. SQLite backup needs root access for legacy files and writable sidecar access even when its connection is mode=ro. Verify exact running SHA, ownership subsets, nondecreasing message counts, and rejected anonymous requests.

On a failed auth/data/version check, stop promotion. Do not reopen the pre-auth production container as an automatic rollback. After the initial cutover, a known authenticated image is the application rollback target. Restoration and DNS changes must run through the Actions lane; retain exact original DNS and infrastructure metadata with the release report.

The dedicated release-request JSON files and workflows use the bounded feat/ia-release-20260914 maintenance branch. Editing promotion-request.json triggers the explicitly selected stage and immutable source SHA; editing smoke-request.json runs the target smoke checks; editing dns-request.json applies only reviewed IA DNS records. Do not trigger them by accident during routine development.

## Validation scope

Public checks verify exact build, login flags, rejected anonymous requests, separate Cognito client/callback, PKCE/state/nonce, secure cookie and Microsoft redirect. Hosted checks use labelled synthetic app identities for invitations, activation, private history/export and revocation. These do not claim a new human Microsoft/MFA login. The prior DEV pilot was confirmed by Jose; staging and production human acceptance follows the completion notice.

## Completed release evidence

The application image `b911a17212e483aa5dd96cb7cae86cb194df7c82` is deployed in all three environments. Final production promotion: Actions 34791893770; full hosted acceptance: 34792075810. All three actual Anthropic model IDs, Bash tools, specialist delegation, application model switching, PRMS queries, invitation/private history/export/revocation and real PDF watermark checks passed. Local checks: 436 backend tests passed, 13 explicitly opt-in live tests skipped; 175 frontend tests and build passed.

The promotion configuration explicitly sets `IA_VOICE_ORIGINS` to the target app origin. Voice status/configuration accepts that origin and rejects foreign origins. The prior September 11 speech pilot remains separate evidence; this promotion did not repeat a microphone/audio session.

Before the final production update, 76 sessions and 3,091 messages (including one baseline QA fixture) were present; the same counts and ownership records survived. A follow-up hash comparison matched all 17 legacy workspace files (8 uploads, 8 outputs, 1 export); all three database files are writable by UID 1000. Final production backup is `/opt/cgiar-ia/release-backups/20260914T001400Z` on i-0f2866b0c6903a70e. These are host paths, not Mac-local deliverables.

The application image remains pinned to b911a17. Later commits contain only test/deployment maintenance and this runbook, so synchronizing them to the development branch does not require another app rollout. The post-release report and 15-minute production observation record are in `/Users/smithai/workspace/outputs/innovation-analytics-release-20260914/`.

Stopped rollback containers have automatic restart disabled (`restart=no`) so they cannot compete with the active app or reopen a retired pre-auth build after an EC2/Docker reboot. The active app retains `restart=always`. Verified on both staging and production through Actions 34792626231; future promotions set this policy immediately after retaining the old container.
